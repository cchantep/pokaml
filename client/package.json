{
  "dependencies": {
    "bs-platform": "1.3.0"
  },
  "scripts" : {
    "prebuild": "rm -rf lib/ && rm -rf shared && mkdir shared && cp ../server/src/foo.ml ../server/src/bar.ml shared/",
    "build" : "bsb"
  }
}
